---
marp: true
theme: default
paginate: true
header: '{{VAULT_NAME}} — {{TOPIC}}'
footer: '{{TODAY}}'
---

# {{QUESTION}}

<!-- One title slide. Subtitle = the atom citation count, e.g. "Based on 6 atoms across 4 sources." -->

---

<!--
USAGE: This is the per-slide template after the title. Repeat for each main point.
Each slide should:
  - Have ONE headline (≤ 8 words)
  - 3-5 bullets max (each ≤ 12 words)
  - End with the citation: [[{lang}/wiki/<atom>]]
  - NEVER mix atoms across slides arbitrarily — group by sub-topic

Slide count budget by regime:
  - A (Narrow Factual): 3 slides max (intro + answer + sources)
  - B (Tactical Multi-Palanca): 5-7 slides (intro + 1 per step + sources)
  - C (Taxonomic / Broad): 8-12 slides (intro + comparison + decision tree + sources)

Last slide is ALWAYS "Sources" listing every cited atom with its locator URL.

When done rendering, save to:
  {{VAULT_PATH}}/{{LANG}}/queries/{{TOPIC}}--{{SLUG}}.marp.md

Then tell the user how to view it:
  npx @marp-team/marp-cli {{output_file}} --preview
  # or open in VS Code with the Marp extension
-->

## Slide N: <headline>

- bullet 1
- bullet 2
- bullet 3

[[{{LANG}}/wiki/<atom-stem>]]

---

## Sources

- [[{{LANG}}/wiki/<atom-1>]] — <one-line claim summary>
- [[{{LANG}}/wiki/<atom-2>]] — <one-line claim summary>
...

<!-- If a conflict caveat applies (CLAUDE.md §4.5), add a final ⚠️ slide before Sources. -->
